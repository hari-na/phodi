# Local image generation dependencies (FLUX.1 schnell NF4 + SDXL Turbo).
# Install PyTorch with CUDA FIRST, separately, matching your driver:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# Then install the rest of these.

diffusers>=0.32.0
transformers>=4.45.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
sentencepiece>=0.2.0
protobuf>=4.25.0
Pillow>=10.0.0
